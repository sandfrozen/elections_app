<%= form_for(@election_candidate, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @election_candidate.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@election_candidate.errors.count, "error") %> prohibited this election_candidate from being saved:</h4>

      <ul>
      <% @election_candidate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :party, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :party, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :list, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :list, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :place, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :place, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :User_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :User_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :Election_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :Election_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
